// Crear un programa que pida al usuario un numero y que devuelva un mensaje diciendo si el numero es par

/* function ingresoNumero(unNumero) {
  let otroNumero = parseInt(prompt(unNumero));
  if (otroNumero % 2 === 0) {
    return document.write("Si el numero es par");
  } else {
    return document.write("El numero no es par o no es un numero");
  }
}
ingresoNumero(); */

/* const ingresoNumero = (unNumero) => {
    let otroNumero = parseInt(prompt(unNumero));
    if (otroNumero % 2 === 0) {
      return document.write("Si el numero es par");
    } else {
      return document.write("El numero no es par o no es un numero");
    }
}

ingresoNumero(); */

/* const primo = (unNumero) => {
    let otroNumero = parseInt(prompt(unNumero));
    if(((otroNumero/otroNumero) ===1 || (otroNumero/1) === otroNumero) || (otroNumero % otroNumero != 1 && otroNumero % 1 != otroNumero)) {
        document.write(`El numero es primo`)
   } else {
    document.write(`El numero no es primo`)
   }
}

primo()
 */