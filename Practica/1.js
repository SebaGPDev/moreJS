// crear un programa que pida a un usuario que ingrese un numero y devuelva un mensaje diciendo si es positivo, negativo o 0

/* function ingresoNumero(unNumero) {
    let otroNumero = parseFloat(prompt(unNumero));
    if (otroNumero > 0) {
        return document.write("El numero que ingreso es positivo");
    } else if (otroNumero < 0) {
        return document.write("El numero que ingreso es negativo");
    } else if (otroNumero === 0) {
        return document.write("El numero que ingreso es 0")
    } else {
        return document.write("No ingreso un numero valido");
    }
}
ingresoNumero(); */

/* const ingresoNumero = unNumero => {
    let otroNumero = parseFloat(prompt(unNumero));
    if (otroNumero > 0) {
        return document.write("El numero que ingreso es positivo");
    } else if (otroNumero < 0) {
        return document.write("El numero que ingreso es negativo");
    } else if (otroNumero === 0) {
        return document.write("El numero que ingreso es 0")
    } else {
        return document.write("No ingreso un numero valido");
    }
}
ingresoNumero(); */

